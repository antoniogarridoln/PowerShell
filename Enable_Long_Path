Esse erro é clássico de limite de path do Windows (260 caracteres) + OneDrive. E você está certo: sair renomeando pastas não é viável em ambiente corporativo.

✔️ Isso sozinho já resolve 90% dos casos
Passo a passo:
Clique no ícone do OneDrive
Settings → Account → Unlink this PC
Faça login novamente
Quando pedir o local → escolha:
C:\OD


Solução 2 — Habilitar suporte a Long Paths no Windows

Windows moderno suporta mais que 260 caracteres, mas nem sempre está ativado.

✔️ Via GPO:
Computer Configuration → Administrative Templates → System → Filesystem
Enable Win32 long paths = Enabled


✔️ Via Registry (rápido) usanod powershell:

New-ItemProperty `
  -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" `
  -Name "LongPathsEnabled" `
  -Value 1 `
  -PropertyType DWORD `
  -Force

⚠️ Importante:

Confirme:

Get-ItemProperty `
  "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" `
  -Name LongPathsEnabled

O resultado esperado é:

LongPathsEnabled : 1


Reiniciar o computador



IMPORTANTE
OneDrive/SharePoint
A Microsoft estabelece um limite de 400 caracteres para o caminho decodificado completo na nuvem. 
Além disso, existem limites separados para nomes de arquivos/pastas e alguns aplicativos antigos continuam presos ao comportamento de 260 caracteres mesmo com LongPathsEnabled.
